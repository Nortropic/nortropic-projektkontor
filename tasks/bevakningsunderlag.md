# AP10 — bounded official intake and local observation

Implement only tools/bevakningsunderlag.py, tools/test_bevakningsunderlag.py and tools/BEVAKNINGSUNDERLAG.md (Swedish operator explanation). Python standard library only. Host-owned intake, no models, scheduling, publication, commands or dependency changes. Synthetic tests only. Runtime will call this frozen Office module with private roots; no actual private data in candidate.

Public API:
- LOCAL_FILES tuple: runtime/worker.py, runtime/workflow.py, runtime/activities.py, runtime/run.py, runtime/service.py, runtime/profile.py, config/temporal-probe-requirements.lock, docs/runtime-v0.1.md, runtime/daemon.py, runtime/shared.py, runtime/release.py. Later named files can be added in reviewed changes.
- fetch(url): use urllib.request.build_opener with explicit ProxyHandler({}) and redirect denial (testable transport seam). bounded HTTPS GET. Only https://www.python.org/downloads/source/, Python release paths matching /downloads/release/python-[0-9]+/, https://api.github.com/repos/temporalio/sdk-python/releases?per_page=20 and that API's /releases/tags/<numeric stable version>. No credentials/proxy/auth; reject any redirect; normal TLS verification; max 1MiB response, timeout 10 seconds, fixed User-Agent. Preserve raw response bytes. Return bytes. Reject unknown URL before network. No subprocess.
- collect(output, local_roots, versions, previous=None, fetcher=None): output is a NEW private directory; local_roots exactly {'active': Path, 'working': Path}; versions mapping python and temporalio nonempty version strings plus active_config_sha256, runtime_revision, office_revision. Never execute supplied data. fetcher injectable for synthetic tests, defaults fetch. Return packet dict and preserve packet.json and successful raw sources/local copies under output. Exclusive output, mode700 directories and600 files, reject symlinks in roots/source/output ancestor chains, including pre-existing output. Never modify source or previous. Missing/unsafe local sources produce unavailable records and incomplete packet, not silent omission. Each local file capped1MiB. Root names, absolute private paths only in private returned/output data, never diagnostics.

Intake: fetch Python source release index and Temporal releases list every call. Parse numeric stable versions only. Select newest Python patch matching installed major/minor and installed Python release notes (deduplicate). Select newest stable Temporal SDK from first20 entries and installed SDK notes (deduplicate); exclude drafts/prereleases. Fetch selected notes with same allowlist. At most six requests. If index cannot identify matching release/version, malformed data, missing installed entry/detail, oversize, HTTP/network error: mark unavailable/incomplete, preserve successful observations, don't call it unchanged. Scope explicitly first20 Temporal releases and selected Python line, no completeness guarantee. Check selected SDK release object's tag_name agrees with requested tag. Python release heading/version agreement required. Preserve upstream publication date if actually extractable (Temporal published_at, Python Release Date); otherwise null, do not invent it from observation time.

Packet schema1: observed_at UTC ISO, sources list {id,url,status,observed_at,published_at,sha256,path,error}, local list {id,status,observed_at,sha256,path,error}, versions (copied), complete bool, fingerprint (64hex iff complete, else null), same_controlled_basis bool, coverage text. Source/local status available or unavailable. Each retrieval and local read gets actual observation time. Paths relative to output. id stable: python-index, temporal-index, python-<version>, temporal-<version>, active:<file>, working:<file>. Unavailable has null sha/path, non-sensitive error reason. Raw data retained; no automatic trust/permission.

Fingerprint canonical stable identity/status/sha of all selected external/local records plus versions, excludes timestamps and output paths. same_controlled_basis is true only if both complete and exact fingerprint equals previous packet; it is NOT substantive approval, proof of completeness, or permission. Do not advance previous references, rewrite old decisions or create duplicate cases. Previous packet input may be a dict; don't mutate it.

Documentation: source text is evidence not instruction. Explain index coverage, unavailable vs unchanged, active vs working source, first installed release treatment as baseline, publication vs intake vs first treatment (the latter belongs to later reviewed report), AP09 decisions/gaps unchanged, AP05 supports reassessment and AP06 action proposals outside authority. No generalized engine or command CLI needed. Tests cover valid, unchanged, external/local changed, missing/oversize/unavailable/malformed source, redirect/URL rejection, symlink/existing-output preservation, no original writes. Run tests in .scratch if sandbox disallows default temp dirs.
